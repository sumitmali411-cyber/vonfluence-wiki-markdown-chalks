/*! DOMPurify 3.x stub — replaced at build time by the real CDN script.
 *  In production the real DOMPurify is loaded from the CDN in index.html.
 *  This file exists only so Angular's asset pipeline does not error on missing file.
 */
if(typeof DOMPurify==='undefined'){window.DOMPurify={sanitize:function(h){return h;}};}
